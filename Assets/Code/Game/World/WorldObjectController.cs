using UnityEngine;

public class WorldObjectController : MonoBehaviour {
    void OnCollisionEnter(Collision collision) {
        //TODO define what and how can world object be destroyed
        //if (collision.gameObject.tag == "Player") {
        //}
    }
}
